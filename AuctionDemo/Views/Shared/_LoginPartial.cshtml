@using Microsoft.AspNet.Identity
@*@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

        <ul class="nav navbar-nav navbar-right">
            <li>
                @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
            </li>
            <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
        </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}*@
<style>
    .fontgrey{
        color:#5D6469 !important;
        font-size:18px;
    }
    .fontgrey:hover{
        background-color:#D6D9DC;
    }
    .trans{
        transition:0.4s;
    }
</style>
<ul class="navbar-nav my-2 my-lg-0">
    @if (Request.IsAuthenticated)
    {
        <li class="nav-item active">
            <a class="nav-link fontgrey trans" style="border-radius:10px;font-size:20px;margin-left:10px;margin-right:8px;" href="~/Account/MyProduct"><i class="fas fa-shopping-basket"></i></a>
        </li>
        <li class="nav-item active">
            @{
                var user = User.Identity.Name.Split('@');
                string userName = user[0];
            }
            @*<a class="nav-link fontgrey trans" style="border-radius:10px;font-size:14px;" data-toggle="dropdown">@userName</a>*@
            
            <a class="nav-link fontgrey trans" style="border-radius:10px;font-size:18px;" href="/Product/">@userName</a>
        </li>

        <li class="nav-item active">
            @*<a class="nav-link fontgrey trans" style="border-radius:10px;" href="~/Account/Logout"><i class="fas fa-sign-out-alt"></i></a>*@

            
            <i class="fa fa-bars  trans" style="font-size:26px;color:#5D6469;margin-left:12px;margin-top:8px;cursor:pointer;" -webkit-text-fill-color:lightsalmon ; border-radius:5px;" aria-hidden="true" data-toggle="dropdown"></i>
            <div class="dropdown">
                <div class="dropdown-menu" style="background-color:#fafafb; border-radius:10px;">
                    <a class="nav-link fontgrey trans fontgrey trans" href="~/Manage/AddPhoneNumber">Telefon Ekle</a>
                    <a class="nav-link fontgrey trans fontgrey trans" href="~/Manage/ChangePassword">Şifre Değiştir</a>
                    <a class="nav-link fontgrey trans fontgrey trans" href="~/Manage/Index">Giriş Ayarları</a>
                    <a class="nav-link fontgrey trans fontgrey trans" href="~/Account/Logout"><i class="fas fa-sign-out-alt"></i> Çıkış Yap</a>

                </div>

            </div>

        </li>
        
        

    }
    else
    {
        <li class="nav-item active">
            <a class="nav-link fontgrey trans" style="border-radius:10px;position:relative;left:70px;" href="~/Account/Login">Giriş Yap</a>
        </li>

        <li class="nav-item active">
            <a class="nav-link fontgrey trans" style="border-radius:10px;position:relative;left:80px;" href="~/Account/Register">Kayıt Ol</a>
        </li>
    }

</ul>
